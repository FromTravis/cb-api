# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.ipynb

# Environment / secrets
.env
.env.local

# TTL cache (regenerated at runtime)
.cache/
# .data/ is committed — contains pre-computed Claude summaries so Railway
# doesn't need to regenerate them on every cold start

# IDE / OS
.DS_Store
.claude/
*.zip
data.xml
*.parquet
Untitled*.ipynb
.ipynb_checkpoints/
files.zip
# Root-level dead duplicates (active versions live in fetchers/)
/fred.py
/eurostat.py
/ecb.py
